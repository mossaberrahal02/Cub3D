#include "cub3d.h"

typedef struct s_all
{
    char **full_map;
    char **map;
} t_all;

void fetch_map(t_all *all)
{
    int fd;
    open()
    while ()
    {
        /* code */
    }
    
    close(fd);
}

void parsing(t_all *all)
{
    fetch_map(all);
}

int main(int ac, char **av)
{
    t_all all;
    parsing(&all);
}